<%= form_for(study_guide) do |f| %>
  <% if study_guide.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(study_guide.errors.count, "error") %> prohibited this study_guide from being saved:</h2>

      <ul>
      <% study_guide.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
